# chassis-document-validator
Python automation built with Selenium and OpenPyXL that validates vehicle records by comparing spreadsheet data against information retrieved from a web system. The automation checks chassis numbers, compares CPF/CNPJ values, validates record status, retries failed searches and updates the workbook automatically.
